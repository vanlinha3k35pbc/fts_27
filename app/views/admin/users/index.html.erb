<% provide :title, "All Users" %>

<h1>All Users</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render 'shared/search_form' %>
  </div>
</div>

<div class="users">
  <%= render @users %>
<div class="users">

<%= will_paginate @users %>
